class Game {
    constructor() {
        this.dots = []

        // 1. add Event Listener that calls addDot
    }

    addDot() {
        // 2. add a new dot to this.dots
    }
}

class Dot {
    constructor(x, y, size = 20, color = '#942942') {
        // 3. create a dom element with the given coordinates (and also other properties)
        // 4. put that dom element on the page
    }
}

new Game()
